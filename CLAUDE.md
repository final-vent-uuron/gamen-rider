# CLAUDE.md

このファイルは、このリポジトリで作業する AI（Claude）向けのプロジェクト前提メモです。
「何を作っているか」「どう流れるか」を共有するのが目的で、細かい技術構成は固定しません（その場で変わるため）。

---

## プロダクト概要

**仮面ライダー龍騎モチーフの IoT 対戦ゲーム**（ハッカソン作品）。

プレイヤーがカードデッキをかざして「変身」し、自分が認証したライダーの姿になって対戦する。
パンチ・キック・移動は身体の動き（加速度センサー）で行い、必殺技「ファイナルベント」はカード＋ポーズで発動する。

- **対戦形式**：個人戦 1v1v1v1（計4人）
- **物理構成**：2台の PC に各2人ずつ。4人が同じアリーナで戦う
- **対戦の見た目**：横スクロール系の 3D 対戦。スマブラのようなイメージ（HP制 / スマブラ風UI）
- **核となる要素**：「ファイナルベント」発動機構（Organization 名 `final-vent-uuron` の通り、ここがプロジェクトの目玉）

---

## ユーザーフロー（全体）

```
[画像認証画面]
   │  自分のカードデッキをカメラにかざす
   │  → ORB 特徴点マッチング（opencv.js）でカード＝ライダーを識別
   ▼
[ポーズ認証]
   │  特定のポーズを取る（MediaPipe Pose 想定 ※未確定）
   │  → カード認証 ＋ ポーズ認証の両方クリアで変身成立
   ▼
[バトル画面]
   │  認証したライダーの姿で対戦（three.js + 3Dモデル）
   │  ・移動：足の加速度センサー
   │  ・パンチ / キック：腕の加速度センサー
   │  ・HP が尽きたら脱落
   │
   │  ┌─ ファイナルベント（必殺技 / ウルト）─────────┐
   │  │  画面右下に PC の web カメラが常時表示          │
   │  │  → ファイナルベントのカードをかざす            │
   │  │  → 特定のポーズを取る                          │
   │  │  → 両方クリアで必殺技が発動                     │
   │  └────────────────────────────┘
   ▼
[決着]
```

---

## 認証フェーズの運用ルール

- **1PC に web カメラは1台**なので、同じ PC の2人は **順番に** カード認証 → ポーズ認証を行う（同時不可）。
- バトル中のファイナルベントは、**打ちたくなった人がその都度撃つ**運用（厳密な占有制御はせず、プレイヤー同士が空気を読む前提）。右下カメラは常時1つ。

---

## 入力デバイス（操作系）

| 操作 | 入力方法 |
|------|----------|
| 移動 | 足に着ける加速度センサー |
| パンチ / キック | 腕に着ける加速度センサー |
| カード認証 | PC の web カメラ（ORB / opencv.js） |
| ポーズ認証 | PC の web カメラ |
| ファイナルベント | 右下の web カメラ（カード＋ポーズ） |

> センサーは「腕・足に着ける自作デバイス」を想定。具体的なハード（マイコン・接続方式）は**未確定**。
> 確定するまでは、入力を抽象化したインターフェース（例：`punch` / `kick` / `move` イベント）で受け取れるようにしておき、入力ソースを差し替え可能にしておくのが安全。

---

## 通信

- PC・デバイス間は **WebSocket** でつなぐイメージ。
- 4人 / 2PC が同じ対戦状態を共有する。
- 対戦ルームは **作成機能は不要**。固定の「固有ルーム」に各自が**参加するだけ**でよい。

---

## まだ決まっていないこと（ハードコードしない）

以下は未確定。仮置き・抽象化で進め、決め打ちした実装を埋め込まないこと。

- **DB**：未定。一旦仮置き（インメモリ or 仮のストアで可）。
- **3Dアセット（ライダーモデル）**：未定（モデルの作成方法も未定）。まずはプレースホルダ（box / capsule など）で対戦ロジックを成立させ、後からモデルを差し替えられる構造にする。
- **加速度センサーのハード/接続方式**：未定（上記参照）。
- **ポーズ認証の判定ライブラリ**：MediaPipe Pose 想定だが確定ではない。

---

## 開発の進め方メモ

- 既存実装は **ブラウザ側（opencv.js）の ORB 特徴点マッチング**が進行中。カード認識はそこに寄せる。
- 「重い認識はブラウザ、状態同期は WebSocket」というゆるい分担を基本にしつつ、構成は固定しない。
- 対戦の見た目は three.js で 3D 描画。ライダーの 3Dモデルは未用意のため、まずプレースホルダで作って後差し替え。
- 新しい機能を足すときは、まず**フローのどの段階か**（認証 / バトル / ファイナルベント）を確認してから実装する。

---

## リポジトリ情報

- リポジトリ：`gamen-rider`
- Organization：`final-vent-uuron`
- 共同開発者：`uuronn`
